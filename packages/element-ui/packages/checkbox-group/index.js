import ToCheckboxGroup from '../checkbox/src/checkbox-group.vue';

/* istanbul ignore next */
ToCheckboxGroup.install = function(Vue) {
  Vue.component(ToCheckboxGroup.name, ToCheckboxGroup);
};

export default ToCheckboxGroup;
