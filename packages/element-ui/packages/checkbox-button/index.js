import ToCheckboxButton from '../checkbox/src/checkbox-button.vue';

/* istanbul ignore next */
ToCheckboxButton.install = function(Vue) {
  Vue.component(ToCheckboxButton.name, ToCheckboxButton);
};

export default ToCheckboxButton;
