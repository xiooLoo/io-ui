import ToButtonGroup from '../button/src/button-group';

/* istanbul ignore next */
ToButtonGroup.install = function(Vue) {
  Vue.component(ToButtonGroup.name, ToButtonGroup);
};

export default ToButtonGroup;
